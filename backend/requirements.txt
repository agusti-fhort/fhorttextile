# FHORT Textile Tech — dependències DIRECTES (top-level) del backend.
# Identificades pels imports reals del codi + apps a settings.py. Versions fixades.
# El freeze complet (incloent transitives) viu a requirements.lock.

# --- Core web ---
Django==6.0.5
djangorestframework==3.17.1
djangorestframework-simplejwt==5.5.1
django-tenants==3.10.1
django-cors-headers==4.9.0
django-filter==25.2
drf-spectacular==0.29.0
psycopg2-binary==2.9.12
gunicorn==26.0.0

# --- Planificació: calendari laboral / festius / dies hàbils ---
workalendar==17.0.0
python-networkdays==1.2

# --- Extracció AI de fitxes tècniques ---
anthropic==0.104.1
httpx==0.28.1

# --- Utilitats ---
openpyxl==3.1.5
Pillow==12.2.0
python-dotenv==1.2.2

# --- Documents comercials (B2): generació de PDF (pure-python, cap dependència nativa) ---
reportlab==4.5.1
# Normalització del logo de l'emissor: rasteritza SVG → PNG per a la capçalera dels documents.
# Depèn de la lib nativa libcairo2 (ja al sistema); només s'importa a l'upload del logo.
cairosvg==2.9.0

# --- Motor de patrons (S1): lectura/escriptura de DXF-AAMA ---
# Nucli del parser. Preserva els tags desconeguts del DXF, que és el que permet
# reproduir l'empremta del CAD d'origen en exportar. Arrossega numpy, fonttools i
# pyparsing; NO s'instal·la l'extra "draw" (portaria matplotlib + Qt: el render SVG
# el fem nosaltres des del model geomètric intern).
ezdxf==1.4.4
