# REVISIÓ INDEPENDENT · C1–C6 (commits 93–100)

> **Data:** 2026-08-07 · **Branca:** `dev` (staging) · **Cap push.**
> **Qui:** sessió M-FI, com a **revisor extern**. La implementació de C1–C6 és de l'altra
> sessió (`ea855527` → `5bae66c2`); aquí **no s'ha tocat ni una línia del seu codi**.
> **Per què existeix:** és l'únic angle que la sessió que escriu una peça no es pot donar a si
> mateixa. Decisió de l'Agus, 07/08.
> **Referència:** `REPORT_CATALEG_TALLES.md` (el report de la implementació).

---

## 0 · VEREDICTE

| Porta | Resultat |
|---|---|
| **verificador** — `manage.py check` | ✅ net (0 issues) |
| **verificador** — `npm run build` | ✅ net (EXIT=0, 0 errors; només l'avís preexistent de mida de chunk) |
| **guardia-i18n** (C5) | ✅ **SENSE VET** — paritat 12/12/12 verificada mecànicament |
| **guardia-ui** (C5) | 🔴 **VET** — chip nou paral·lel + 4 hex amb token exacte disponible |
| **revisor-diff** (C1–C4, C6) | 🔴 **3 BLOQUEJANTS** — totes tres a C4, cap a les migracions (§4-bis) |
| fum `qa_n_capes_run.py` | ✅ verd, corregut independentment (§4) |
| **auditoria de dades** (independent) | ⚠️ 1 troballa nova a `public` (§3.1) |

**El verd NO es pot declarar**, per dues portes:

1. **`guardia-ui` veta C5** — chip nou paral·lel + hex amb token exacte disponible. Els dos
   motius verificats per mi (§2).
2. **`revisor-diff` troba 3 bloquejants, TOTES a C4** (§4-bis), i totes tres tenen la mateixa
   arrel: **la constraint nova és correcta, però els camins vius que escriuen `ordre` no s'han
   adaptat**, i el «fre» de `SizingProfile` és codi mort. Verificades per mi una a una.

**C1 · C2 · C3 · C6-pas-1 passen tots els controls.** Les migracions —que era on es buscava el
risc, i amb raó— **estan netes**: cap creua per `id`, totes són idempotents, i l'esborrat de
`0066` està contrastat amb SQL als tres schemes amb el cens complet de cascades.

> 🔑 **La lliçó del sprint, en una línia:** el risc no era on tots miràvem. Les migracions, que
> feien por, estan ben fetes; el que ha quedat obert és **el codi de sempre que ara viu sota una
> regla nova**.

**I dues aportacions que no eren a l'encàrrec** però que responen preguntes que el report deixava
obertes per a l'Agus i la Montse: §3.2 corrobora el valor interpolat de C1 contra una sèrie
independent, i §3.3 **contradiu** amb dades el valor triat a C2.

---

## 1 · El que he verificat abans de res: el restart

Era la primera instrucció del brief i estava ben diagnosticada.

```
systemctl restart ftt-staging   →   active · ActiveEnterTimestamp 2026-08-07 06:08:52 UTC
```

Rutes vives després (sense credencial; **401 = viva**, 404 = el procés no la té):

```
401  GET   /api/v1/models/            401  GET   /api/v1/size-systems/
401  GET   /api/v1/garment-types/     401  GET   /api/v1/size-definitions/
401  PATCH /api/v1/models/1/update-step2/   401  POST /api/v1/models/1/pom/1/regim/
```

> ⚠️ **Dos 404 que NO eren un problema.** Els meus primers intents (`/api/v1/pom/size-systems/`)
> van donar 404 perquè el router de `pom` penja de `/api/v1/` **sense segment `pom/`**. Ho
> deixo escrit perquè aquesta casa té la llei «404 = el backend desplegat no té la ruta», i
> aplicar-la a un URL mal endevinat porta a diagnosticar un problema que no existeix.

**El build:** `frontend/dist` era de les 05:57 i C5 es va commitar a les 06:00 — semblava ranci.
**No ho era:** el build va sortir del disc just abans de commitar. Ho he comprovat pel contingut,
no per la data (`grep` de `restrictions_title`, `Restriccions del run`, `layer_construction` i
`no exclou ningú` dins `dist/assets/`) → **hi són tots**. Amb l'arbre de front net i 0 fitxers
de `frontend/src` més nous que el `dist`, he refet el build igualment: reprodueix el mateix
estat i surt net.

---

## 2 · 🔴 EL VET DEL `guardia-ui` (C5 · `b406cdfc`)

Els dos motius, verificats per mi contra el codi real:

### 2.1 · Un chip NOU quan el brief deia «els mateixos chips de N2, cap layout nou»

`RunRestrictionEditor.jsx:18-24` escriu una factoria d'estil `chip()` pròpia. Però
`components/grading/wizardUI.jsx:25` ja exporta un `Chip` amb el contracte exacte que calia
(`{active, onClick, disabled, motiu, children}`) **i ja s'usa per a les mateixes capes**:

- `pages/ModelWizard.jsx:637-639` → tria de **target**
- `components/grading/GraduacioPanel.jsx:136` → tria de **fit**

El propi `wizardUI.jsx` porta escrit al capdamunt que el component ha de ser un de sol «**MAI
una còpia**». N'hi havia tres de reutilitzables (`Chip`, `TargetPills`, `GroupPills`) i la peça
n'ha fet una quarta variant, amb geometria i color diferents dels tres.

**Conseqüència mesurable, no estètica:** l'estat «triat» del chip nou és `--gold` sobre
`#f5e6d0` ≈ **2.8:1** de contrast a mida `--fs-label`; el parell de la casa (`--warn` sobre
`--warn-bg`) dona ≈ **5.9:1**. WCAG AA demana 4.5:1. I el color **és l'únic senyal de
seleccionat** que hi ha (no hi ha `aria-pressed`).

### 2.2 · Hex hardcoded amb el token exacte disponible

`CLAUDE.md`: «Colors via tokens CSS, mai hex (única excepció: `KONVA_COL`)». Al delta no hi ha
Konva. Set literals hex, quatre amb token **exacte** ja definit a `index.css`:

| Literal | Vegades | Token que existeix |
|---|---:|---|
| `#f5e6d0` | 3 | **`--gold-pale`** (`index.css:17`) |
| `#a32d2d` | 1 | **`--err`** (`index.css:45`) |
| `#e0c8a0` | 2 | cap (propagat de N2, `RunRestrictionTags.jsx:64`) |
| `#fdfaf6` | 1 | cap — i els tres panells germans de la MATEIXA pantalla usen `#fdf9f5` |

### 2.3 · La correcció, si la voleu

`import { Chip } from './grading/wizardUI'` + esborrar `chip()` tanca **de cop** el veto de
layout i el de contrast; i canviar `#f5e6d0`→`var(--gold-pale)` (×3) i `#a32d2d`→`var(--err)`.
Els dos hex sense token (`#e0c8a0`, `#fdfaf6`) demanen decisió: batejar-los o alinear-los.

**No ho he tocat**: la peça és de l'altra sessió, que és viva i té els mateixos fitxers oberts.

### 2.4 · El que C5 SÍ que fa bé, i s'ha de dir

- Els noms del PATCH casen exactament amb el serializer (`target_codis`, `grup_codis`,
  `construccio_codis`, `fit_codis` — atenció a la grafia catalana de `construccio_`), i és un
  **PATCH**, no un PUT: no hi ha desat mut d'altres camps.
- L'editor **se sembra bé** (`SizeSystemLightSerializer` ja exposa les 4 llistes), o sigui que
  no hi ha el clàssic «obre buit i desa el buit».
- La porta `CONFIGURE` existeix de debò (`pom/views.py:84-87` gateja `partial_update`).
- Els grups es llegeixen **de la BD**, no de la constant del front, i el comentari del codi
  (`:58-60`) demostra que era una decisió conscient. Això és exactament la llei de C6.

---

## 3 · L'AUDITORIA DE DADES (independent — no era a l'encàrrec de cap guarda)

He contrastat el que el report **afirma** amb el que la BD **diu**, als tres schemes, creuant
sempre per `codi`.

### 3.1 · 🚩 LA TROBALLA: `public.TODDLER_EU` conserva la mateixa corrupció que C1 va reparar

C1 va reparar `fhort.36`. El report classifica `public.8` com a «sèrie monòtona → **intacte**».
**És monòtona, i és falsa.** La prova no és l'ull, és el creuament amb una sèrie independent al
**mateix bust**:

| schema · sistema | talla | bust | waist | hip |
|---|---|---:|---:|---:|
| `public` · KIDS_EU | 6Y | 60 | 54 | 64 |
| `fhort` · KIDS_EU | 6Y | 60 | 54 | 64 |
| `fhort` · TODDLER_EU **(reparat)** | 116 | 60 | **58** | **67** |
| `public` · TODDLER_EU **(intacte)** | 116 | 60 | **34** | **40** |

I no és una comparació forçada: `TODDLER_EU` 116 i `KIDS_EU` 6Y **cobreixen exactament la
mateixa franja d'edat** (`age_months` `[72-84]` totes dues). Al mateix bust i la mateixa edat,
`public` diu **20 cm menys de cintura i 24 cm menys de maluc**.

**Per què va passar el control:** a `fhort` la corrupció era UNA fila enmig d'una sèrie sana, i
per tant trencava la monotonia i saltava a la vista. A `public` la columna sencera de
waist/hip està desplaçada, i **una corrupció uniforme és monòtona**. El test de salut que es va
fer servir (monotonia) no la pot veure.

⚠️ **Per què importa:** `public` és el catàleg que sembra els tenants nous, els camps es
**veuen** (`SizeSystemDrawer` editable, `SizeSetDetail`, CSV de S8) i **viatgen** al paquet
LOSAN (`export_losan_package.py:285`). No és una columna morta.

🚩 **Decisió de l'Agus:** ¿s'estén la reparació de C1 a `public.8`? (Cap dany viu avui: `public`
no té models.) Ho anoto i **no ho toco** — l'abast d'aquesta sessió és revisar.

### 3.2 · ✅ El valor interpolat de C1, corroborat (per a Agus i Montse)

El report demanava validar `waist=58` / `hip=67` per a la talla 116. **La casa mateixa els
corrobora**: a la mateixa edat i el mateix bust, `KIDS_EU` 6Y diu 54/64. Els valors triats són
del bon ordre de magnitud (els vells, 34/40, eren 20 cm avall).

🚩 **Matís per a la Montse:** els nous queden **4 cm i 3 cm PER SOBRE** de la fila `KIDS_EU` 6Y,
que és la **mateixa franja d'edat** `[72-84]`. Si els dos sistemes s'han de tocar en aquell
punt, els números serien 54/64. La interpolació és correcta *dins de la seva sèrie*; l'encaix
*entre sèries* és la teva paraula.

🚩 I el `body_bust_cm = 60` que el report ja marcava: confirmat que trenca la monotonia de la
seva pròpia sèrie (…104:59 · 110:**61** · 116:**60**). Segueix obert.

### 3.3 · 🚩 El valor de C2 **el contradiuen les dades de la casa**

El report diu: «*L'última no té «següent» de qui sortir. S'ha aplicat el pas local (24 + 6 =
**30**). Si la Montse diu 36, és una línia.*»

**Dos sistemes independents del mateix schema diuen 36**, i tots dos coincideixen a la banda
anterior:

| sistema | banda anterior | **la banda en qüestió** |
|---|---|---|
| `BABY_EU_CM` | 86 `[18-24]` | 92 **`[24-36]`** |
| `TODDLER_EU` | 86 `[18-24]` | 92 **`[24-36]`** |
| `BABY_MONTHS` (C2) | 18M `[18-24]` | 24M **`[24-30]`** ← el que s'ha escrit |

El pas local (+6) és una regla defensable; el catàleg de la casa, però, fa servir **24-36** per
a la banda que segueix 18-24, dues vegades i sense excepció. 👉 **Recomanació: 36.** És la línia
que el propi report ja preveia.

### 3.4 · ✅ La resta de l'auditoria, neta

- **C1** `fhort.36`: `base_unit='CM_HEIGHT'` · `tipus_escala='ALTURA'` · ordres `[1,2,3,4,5,6]`
  únics i seqüencials ✅
- **C2** `BABY_MONTHS` 9 talles, ordres `1..9` únics; `BABY_MONTHS_COM` (42) intacte amb les
  seves 5 ✅ — els dos jocs, efectivament separats.
- **C3**: `MEN-SHIRT-NUM` i `WOMAN_BRW_01` fora ✅ · `TGIRL-EU-HEIGHT` **hi és** ✅ · cap
  ruleset «Prova BRW» ✅
- **C4**: `UNIQUE (size_system_id, ordre)` presenta a **`fhort`, `los` I `public`** ✅
  (auditat a `pg_constraint`, no a la sortida de `migrate_schemas`)
- **C6 pas 1**: `fhort` 21/21 amb FK, 0 forats, **0 divergents** (string vs FK) ✅ ·
  `public` 0 GarmentType ✅ · `los` **1 forat**, confirmat i explicat (sota)

> 🔵 *Una nota sobre el meu propi mètode:* el primer guió d'auditoria donava «!!! REVISAR» per a
> `TGIRL-EU-HEIGHT` a `los` i `public`. **Era un error meu**, no de C3: aquell run només ha
> existit mai a `fhort`. Ho deixo escrit perquè un cens que assumeix simetria entre schemes
> menteix, que és precisament la lliçó de tot aquest sprint.

### 3.5 · ✅ Per què C3 es va aturar: confirmat, i era important

`fhort` · `TGIRL-EU-HEIGHT` (id=6, 8 talles) → **350 `GradingRule`** ancorades a les seves
talles via `talla_base` (`PROTECT`), repartides en 10 rulesets del catàleg canònic:

```
77 EU Woven Woman Relaxed 61 · 78 Oversized 61 · 91 Numeric 61 · 80 Knit Woman Slim 40
85 Woven Man Slim 35 · 87 Knit Baby Regular 25 · 89 Knit Kids Regular 20
82 Stretch Bodycon 19 · 88 Knit Toddler Regular 19 · 92 Woven Dress Flared 9
```

L'aturada va ser correcta i el diagnòstic també: **l'ús d'un run penja de les seves TALLES, no
del run**, i un cens que només mira les FK cap al `SizeSystem` el dona per «risc zero».

---

## 4 · EL FUM READ-ONLY, RE-CORREGUT PEL MEU COMPTE

`ops/qa/qa_n_capes_run.py`, corregut de nou i de forma independent:

```
N1a · 26 runs · classificats 26 · sense classificar 0 []
N1b · GET size-systems/ 200 · 26 files · capes exposades OK
N1b · filtre tipus_escala=ALPHA  → 7 runs, tots ALPHA
N1b · filtre tipus_escala=ALTURA → 3 runs, tots ALTURA
N2  · GET sizing-profiles/?target=WOMAN 200 · 11 perfils · el run hi porta les seves capes
N3  · ordre amb client BRW · target WOMAN: ALPHA_EU_W › NUMERIC_EU_W › ALPHA_EU_M › BABY_EU_CM
N3  · ordre sense client   · target WOMAN: ALPHA_EU_W › NUMERIC_EU_W › ALPHA_EU_M › BABY_EU_CM

✅ FUM VERD · cap escriptura feta
```

Coincideix amb el que el commit `091c2bdf` cita: **la seva correguda era bona**.

> 🔵 **Un parany de mètode, i és meu.** El primer cop el vaig invocar
> `manage.py shell < ops/qa/qa_n_capes_run.py` → **EXIT=0, zero sortida, i cap comprovació
> executada**: el fitxer acaba amb `if __name__ == '__main__'`, que sota `shell <` mai és cert.
> Un fum que no imprimeix res no és un fum verd, és un fum que no ha corregut. La invocació bona
> és `python ops/qa/qa_n_capes_run.py`. Ho deixo escrit perquè el proper que el corri no s'hi
> deixi enganyar com jo.

---

## 4-bis · `revisor-diff` (C1–C4, C6)

### ✅ On es buscava el risc, NO n'hi havia: les migracions estan netes

El perill número u d'aquest sprint era que una migració de dades creués per `id` en un catàleg
on **els ids no coincideixen entre schemes**. Les cinc, llegides línia a línia:

| Migració | Com creua |
|---|---|
| `0064` | `filter(codi='TODDLER_EU')` |
| `0065` | `codi='BABY_MONTHS'` / `'BABY_MONTHS_COM'` |
| `0066` | `filter(codi=codi)` |
| `0068` | `per_codi = {g.codi: g.pk}` → `per_codi.get(gt.grup.strip())` |

**Cap creua per id.** Els literals `26`/`53` de `0066` van en la direcció **bona**: el `get` és per
codi i l'id només s'usa com a **asserció** (`RuntimeError` si no casa) — l'invers de l'antipatró.
Idempotència verificada als quatre `RunPython` (guards a cada branca). L'esborrat de `0066`
contrastat amb SQL als tres schemes, amb el cens complet d'`on_delete`: els dos `SET_NULL`
silenciosos (`models_app.Model.size_system` i el self-FK `parent`) **els cobreix `_penjats`**, i
el delete hauria abortat si haguessin donat ≠0. `0068`: cens de 10 escriptors de `GarmentType.grup`,
**cap bypassa la FK**, i `bootstrap_tenant` **remapeja el pk** en comptes de copiar-lo cru — que
era l'únic lloc on un id d'un altre schema podia viatjar.

### 🚨 On NO es buscava, sí que n'hi ha: C4 posa una constraint i no adapta els dos camins vius que la poden violar

La constraint `UNIQUE(size_system_id, ordre)` és **correcta** i no hi ha cap fila que la violi.
El problema és que **el codi que escriu `ordre` no s'ha tocat**, i la llei de la casa diu que
**els forats a `ordre` són legítims** (`grading_utils.py:374`: «Els forats són LEGÍTIMS i es
conserven»). Els dos camins:

**🚨 A · `size_map_views.py:805-817` — CLONAR renumera IN-PLACE.** Un
`update_or_create(..., defaults={'ordre': …})` dins d'un bucle és exactament el que `0064` i
`0065` van haver d'esquivar amb el two-pass a la banda 1000+. Qualsevol payload que desplaci un
`ordre` (una fila esborrada, una reordenació, una etiqueta intercalada) col·lisiona
**transitòriament** → `IntegrityError` dins del `transaction.atomic` → **500 i rollback de tot el
desat del wizard**. El front hi arriba de debò: `SizeMapSetup.jsx:381` renumera sobre una graella
on l'usuari pot esborrar i afegir files.
*Repro:* clonar un run → esborrar una fila al pas 3 → Desar.

**🚨 B · `SizeSystemDrawer.jsx:142-159` — «Afegir talla» xoca amb els forats, i falla EN SILENCI.**
`handleAdd` envia `ordre: definitions.length + 1`. Amb ordres `1·2·4·5` (4 files), calcula
`ordre=5`, que ja existeix → **400**. I com que només hi ha la branca `if (res.ok)`, sense `else`,
**el botó no fa res i no diu res**.
*Repro:* run amb 5 talles → esborrar-ne una → «Afegir talla».

**🚨 C · El «fre» de §7.4.2 no existeix: els dos `clean()` de C4 són CODI MORT.**
El comentari del propi C4 diu «*Mentrestant el fre viu a `clean()`, aquí sota*»
(`models.py`, `SizingProfile.Meta`). Però **ningú crida `clean()`**, i ho he comprovat de les
quatre maneres:

- cap `full_clean()` a tot `fhort/` fora de `tenants/` i migracions,
- `pom/admin.py` són 3 línies **sense cap `register`**,
- DRF **no** crida `Model.clean()` (cosa que la casa ja sap i resol explícitament a
  `tasks/serializers_b.py:272`, `commerce/serializers.py:398`…),
- els 5 escriptors vius fan `SizingProfile.objects.create(...)` / `SizingProfile(...)`
  (`size_map_views.py:1008`, `s2_views.py:240`, `crea_sizing_profiles.py:125`,
  `reseed_tenant_fhort.py:446`).

O sigui: **res no frena un sisè duplicat**. Els 5 que hi ha segueixen vius (confirmat: `288·510`
i `539·540·541`).

> ✅ **El que SÍ que funciona de C4**, i s'ha de dir: `validate_tipus_escala`
> (`serializers.py:147-163`) és un `validate_<camp>`, i **DRF sí que els crida**. El guard de
> §7.4.3 és viu per la porta de l'API. I la constraint de §7.4.4 la garanteix la BD.
> Dels tres guards anunciats: **un real (BD), un real només per API, i un inert.**

### 🚩 La resta

- **D · `0066:26-30` pot avortar `migrate_schemas` a PROD.** Si allà `MEN-SHIRT-NUM` o
  `WOMAN_BRW_01` existeixen amb un id ≠ 26/53, l'asserció peta i `0067`+`0068` **no s'apliquen**.
  Comprovació abans del deploy: `select id,codi from pom_sizesystem where codi in (…)` a cada
  schema del dump.
- **E · `ordre` passa a ser OBLIGATORI** a `POST /api/v1/size-definitions/`: el
  `UniqueTogetherValidator` de DRF l'exigeix encara que el camp tingui `default=0`. La SPA sempre
  l'envia; altres cridants i tests, no necessàriament.
- **F · `grup` i `grup_ref` poden divergir sense que res ho detecti** (renombrar un
  `GarmentGroup.codi`), i **`los` es quedarà NULL per sempre**: `0068` ja hi està aplicada, o sigui
  que sembrar-hi el vocabulari més tard no farà backfill. 👉 **El pas 2 de C6 necessita un
  backfill NOU i re-runnable; no pot comptar amb `0068`.**
- **G · `grup_ref` és `PROTECT`**: esborrar un `GarmentGroup` referenciat queda bloquejat a la BD
  (abans es podia, deixant strings orfes). Mitigant: `GarmentGroupViewSet` és read-only.
- 🔵 `reverse_code` buits a les 4 migracions de dades (documentat a posta, però és una
  reversibilitat que menteix) · `grup_ref` és escrivible per l'API i `save()` el sobreescriu en
  silenci · el `filter(codi=…)` de `save()` és case-sensitive.

### 🚩 Higiene de commits (per a tu, que ets qui puja)

**El canvi de model de C6 viu dins del commit de C4.** `d139324d` (C4) porta
`GarmentType.grup_ref` + `GarmentType.save()` sense dir-ne res al missatge; `cb938566` (C6) porta
**només** la migració `0068`. Efecte: al commit C4 l'arbre tenia un camp de model **sense
migració** (`makemigrations --check` hi hauria estat vermell). **A HEAD l'arbre és consistent** —
però un `git bisect` o un revert per commits deixaria el repo trencat. Sona a l'amenda d'una
sessió concurrent (la llei de `ftt-dev-concurrent-git`).

---

## 5 · EL QUE QUEDA A LA TAULA DE L'AGUS

| # | Decisió | On |
|---|---|---|
| 🚨 0a | **CLONAR un run i desar pot fer 500** amb rollback del wizard (constraint nova + renumeració in-place). El camí té repro. | §4-bis A |
| 🚨 0b | **«Afegir talla» falla en silenci** quan el run té forats a `ordre` (que són legítims per llei). | §4-bis B |
| 🚨 0c | **El fre de `SizingProfile` no existeix** (els `clean()` són codi mort): res impedeix un sisè duplicat. | §4-bis C |
| 🔴 1 | **El vet de C5**: es corregeix el chip i els hex (petit) o s'accepta l'excepció amb acta? | §2 |
| 🚩 1b | **`0066` pot avortar `migrate_schemas` a PROD** si els ids hi són uns altres. Comprovar contra el dump abans de pujar. | §4-bis D |
| 🚩 1c | **C6 pas 2 necessita un backfill NOU i re-runnable**: `los` es quedarà NULL per sempre (`0068` ja aplicada). | §4-bis F |
| 🚩 2 | **`public.TODDLER_EU`** conserva la corrupció de waist/hip. S'hi estén C1? | §3.1 |
| 🚩 3 | **C1 · talla 116**: 58/67 queden 4 i 3 cm per sobre de `KIDS_EU` 6Y, mateixa edat. Montse. | §3.2 |
| 🚩 4 | **C1 · `body_bust_cm=60`** trenca la monotonia (110 en té 61). Montse. | §3.2 |
| 🚩 5 | **C2 · 24M**: la casa fa servir `24-36` dues vegades; C2 ha escrit `24-30`. Recomanat 36. | §3.3 |
| 🚩 6 | Les 5 obertes que el report ja portava (TGIRL, `SizingProfile`, C6 pas 2, …) | `REPORT_CATALEG_TALLES.md` |
