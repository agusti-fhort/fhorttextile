# GATE DE TREN · nit 25→26/08 · **ATURAT A PAS 0 — NO EXECUTAT**

> Aquest fitxer NO és el report d'un gate. És l'acta d'una **aturada reglamentària**
> abans de llançar res. La suite backend, el `npm run build` i l'`eslint` **NO s'han
> corregut**. No hi ha xifres perquè no hi ha hagut correguda.

## PAS -1 · Identitat — ✅ CORRECTA

| comprovació | valor |
|---|---|
| `hostname` | `fhort-assessment` |
| `systemctl show ftt-staging.service -p WorkingDirectory` | `/var/www/ftt-staging/backend` |

## PAS 0 · Base — ❌ **EL MERGE D'AGUS NO HI ÉS**

L'ordre exigeix que el HEAD de `dev` porti el merge de `f2-garment-override`. **No el porta.**

| fet | valor |
|---|---|
| HEAD de `dev` | `55e76c5d` — `Merge branch 's46-fix-desplegat' into dev` (25/08 11:12 UTC) |
| tip de `f2-garment-override` | `36985004` — `docs(F2): acta del fix…` (25/08 **13:27 UTC**) |
| `git merge-base --is-ancestor f2-garment-override HEAD` | **NO** |
| `git merge-base --is-ancestor 48088b27 HEAD` | **NO** |
| `git branch --contains f2-garment-override` | només `f2-garment-override` |
| M4+M5-dia (`64832c43`) a HEAD? | **SÍ** ✅ |

**El tip de F2 és 2 h 15 min POSTERIOR al HEAD de `dev`.** La branca hi té dos commits
que `dev` no ha vist mai:

- `48088b27` — `fix(F2): el garment entra al set_size_override_view — lookup alineat amb la unicitat real`
- `36985004` — `docs(F2): acta del fix + la correcció del cens`

### No és un cherry-pick amb un altre hash: el contingut tampoc hi és

S'ha comprovat pel CONTINGUT, no només per l'hash — `git diff HEAD f2-garment-override`
sobre els dos fitxers que toca `48088b27`:

- `backend/fhort/models_app/test_f2_garment_override.py` → **fitxer NOU** al diff (343 línies).
  A `dev` no existeix.
- `backend/fhort/models_app/views.py` → a `dev`, `set_size_override_view` encara **no llegeix
  el `garment`**: no hi ha ni la línia `garment = _identitat_de_mesura(data)[2]` ni el
  docstring d'F2.

O sigui: **el defecte que F2 repara segueix VIU al codi de `dev`**, i el banc de 343 línies
que l'hauria de guardar **no entraria a la suite d'aquesta nit**. Un gate corregut sobre
aquest HEAD hauria donat verd sense haver mesurat mai la feina que el tren de demà transporta.

### Per què això sol ja invalidava el gate (context del defecte)

Segons l'acta de la pròpia branca: `ModelGradingOverride` té una `unique` de SIS columnes
—`(model, pom, size_label, capa, instancia, garment)`— i `set_size_override_view` en deia
CINC. Amb dues peces vives que comparteixin POM, el `filter` casa DUES files. La població
que ho exposa **ja existeix a `fhort`** (1320/904, 1379/962, 1380/962).

⚠️ Nota per a qui reprengui: la branca documenta que `set-size-override/` **no té ruta HTTP**
des de D5 (21/07) — la vista es conserva com a vehicle de bancs. El defecte és de **codi viu
en una porta desendollada**, no un 500 abastable avui. Això no canvia el veredicte del gate:
la feina no hi és i no s'ha mesurat.

## Convivència amb la ingesta — ✅ RESPECTADA I INTACTA

Aquesta sessió **no ha llançat cap procés nou**, no ha reiniciat res i no ha tocat
`ftt_corpus` ni el driver. Tot el que s'ha fet és lectura (`git`, `ps`, `psql` en només
lectura). Estat en tancar:

```
3937402  bash /root/gcd_corpus/scripts/run_fetch.sh          (up 16:55)
3937623  bash /root/gcd_corpus/scripts/run_ingest.sh         (up 15:33)
3939960  fetch_batch.py  --batch garments_5000_5   nice 15  R  (00:51)
3940194  ingest_batch.py --batch garments_5000_4   nice 15  R  (00:25)
```

- `postgresql` = `inactive` (paraigua buit, com sempre) · `postgresql@18-main` = **active** (5433). ✅
- Cap connexió morta: `pg_stat_activity` només mostrava la del propi `psql` de la comprovació.
- **PostgreSQL NO s'ha reiniciat. `ftt-staging.service` NO s'ha reiniciat.**

## Què NO s'ha fet, expressament

- ❌ No s'ha fet el merge de `f2-garment-override` a `dev` (no és feina de la nit, i l'ordre
  diu «cap fix nocturn»).
- ❌ No s'ha corregut la suite sobre un HEAD que no és la base demanada.
- ❌ Cap commit, cap push. L'arbre de treball té modificacions d'altres sessions concurrents
  i s'ha deixat **exactament com estava**.

## VEREDICTE

> **NO APTE — gate NO executat.** El HEAD de `dev` (`55e76c5d`) no conté el merge de
> `f2-garment-override`; falta `48088b27` (el fix) i el seu banc de 343 línies. Cal que Agus
> integri la branca a `dev` i **re-llançar el gate sencer** sobre el HEAD resultant.
